# HPA-622 final fix report

Status: DONE

Base: `95a86c9` (`feat(ui): add aggregate traffic overlay`)

## Findings addressed

- Formatted `src/runtime/backend/types.ts` with the repository Prettier shape.
  The full `bun run format:check` gate now passes.
- Added `due_commute_uses_direct_bus_at_zero_flow_and_car_after_bus_congestion`
  to `crates/caelum-core/tests/trip_lifecycle.rs`. The fixture creates a real
  bus route through `GameEngine` and drives a due commute through
  `tick_trips`, the production spawn seam. With no active cars it asserts one
  direct Bus leg is selected and car flow is empty. With twelve active Driving
  trips on the captured bus path, live bus congestion makes the same due
  commute select `Driving` with a private-car payload.
- Added `expect(c.fill).not.toHaveBeenCalled()` to the Driving case in
  `tests/render/citizenRenderer.test.ts`, completing the no-arc/no-fill proof.
- Replaced the new result-oriented `expect` calls in `traffic.rs` and
  `trip_lifecycle.rs` with explicit assertions and guarded access. Remaining
  HPA-622 `expect` calls in these tests are fixture construction only
  (tile/topology/captured-route setup); unrelated pre-existing lifecycle test
  expectations were left untouched.

## TDD RED/GREEN evidence

- The first zero-duration version of the integrated test correctly exposed a
  fixture mistake: a spawn-only tick leaves a non-car trip `Idle` with no route
  plan (`left: Idle`, `right: Waiting`). The test was corrected to advance one
  second through normal trip planning.
- RED behavior probe: after the test existed, the live bus ETA call in
  `router.rs` was temporarily replaced in the working copy with stored
  free-flow path time. The focused command then failed at the congestion case:
  `left: Some(Waiting)`, `right: Some(Driving)`. This temporary production edit
  was restored exactly and is not part of the final diff.
- GREEN: with the live ETA code restored,
  `cargo test -p caelum-core --test trip_lifecycle due_commute_uses_direct_bus_at_zero_flow_and_car_after_bus_congestion`
  passed (`1 passed; 48 filtered out`).

## Verification

All commands ran from `/Users/chanwaichan/workspace/Caelum/.worktrees/hpa-622`.

| Command | Result |
| --- | --- |
| `cargo test -p caelum-core --test traffic --test trip_lifecycle` | PASS; 13 traffic + 49 lifecycle tests |
| `bunx vitest run tests/render/citizenRenderer.test.ts` | PASS; 10 tests |
| `cargo fmt --all --check` | PASS |
| `git diff --check` | PASS |
| `bun run format:check` | PASS; Prettier and Rust fmt |
| `bun run check` | PASS; TypeScript and svelte-check, 0 errors/0 warnings |
| `bun run test:unit` | PASS; 54 files / 702 tests |
| `cargo test --workspace` | PASS; all workspace tests and doctests |
| `cargo clippy --workspace --all-targets -- -D warnings` | PASS |
| `bun run lint` | PASS; ESLint, Stylelint, and Rust clippy |
| `bun run build` | PASS; release WASM, Svelte check, and Vite build |
| final focused commute regression | PASS; 1 test |

The first parallel invocation of `bun run check` and `bun run test:unit`
contended on the shared WASM build and produced a transient Cargo metadata
parse error. The commands were rerun sequentially; both passed with WASM
artifacts up to date.

## Files changed

- `crates/caelum-core/tests/traffic.rs`
- `crates/caelum-core/tests/trip_lifecycle.rs`
- `src/runtime/backend/types.ts`
- `tests/render/citizenRenderer.test.ts`
- this report

## Self-review

- No gameplay production code was changed; the integrated test calls existing
  engine, route, traffic, and trip-tick paths rather than inventing a RoutePlan
  or testing a private helper.
- The bus fixture uses a captured route generated by `CreateRoute`; only the
  current topology is made into a long detour so the test isolates the live
  bus-congestion versus car-choice interaction.
- The renderer assertion checks the actual canvas `fill` spy in addition to
  the existing style and arc assertions.
- No Svelte source was touched, so Svelte-specific implementation guidance was
  not needed.

## Concerns

No known behavioral concerns. `bun run build` emitted the repository's existing
wasm-pack update/metadata recommendations and Vite's existing browser
externalization notice; none affected the exit status or changed files.
