node_modules/
dist/
coverage/
target/
test-results/
playwright-report/
.playwright-mcp/
.worktrees/
.superpowers/
.env
.env.*
.claude/settings.local.json

# WASM artifacts are generated by `bun run wasm:build` from crates/caelum-wasm.
# Never commit them — CI regenerates in every job that needs them so the
# browser always ships Rust core logic matching the PR.
src/generated/caelum_wasm/
