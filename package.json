{
  "name": "caelum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "predev": "test -f src/generated/caelum_wasm/caelum_wasm.js || bun run wasm:build",
    "prebuild": "bun run wasm:build:release",
    "build": "svelte-check --tsconfig ./tsconfig.json && tsc --noEmit && vite build",
    "check": "tsc --noEmit && svelte-check --tsconfig ./tsconfig.json",
    "lint": "bun run lint:svelte && bun run lint:rust",
    "lint:svelte": "eslint .",
    "lint:rust": "cargo clippy --workspace --all-targets -- -D warnings",
    "lint:staged": "lint-staged",
    "format": "prettier --write . --ignore-unknown && cargo fmt --all",
    "format:check": "prettier --check . --ignore-unknown && cargo fmt --all --check",
    "rust:test": "cargo test --workspace",
    "rust:check": "cargo check --workspace",
    "test": "vitest run",
    "pretest": "test -f src/generated/caelum_wasm/caelum_wasm.js || bun run wasm:build",
    "test:unit": "vitest run --project ui --project runtime",
    "pretest:unit": "test -f src/generated/caelum_wasm/caelum_wasm.js || bun run wasm:build",
    "test:unit:coverage": "vitest run --project ui --project runtime --coverage --coverage.provider=v8 --coverage.reporter=lcov",
    "pretest:unit:coverage": "test -f src/generated/caelum_wasm/caelum_wasm.js || bun run wasm:build",
    "test:e2e": "playwright test",
    "wasm:build": "wasm-pack build crates/caelum-wasm --target web --out-dir ../../src/generated/caelum_wasm --out-name caelum_wasm --dev",
    "wasm:build:release": "wasm-pack build crates/caelum-wasm --target web --out-dir ../../src/generated/caelum_wasm --out-name caelum_wasm --release",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "svelte": "^5.26.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.52.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.4",
    "@tauri-apps/cli": "^2.5.0",
    "@testing-library/jest-dom": "^6.6.4",
    "@testing-library/svelte": "^5.3.1",
    "@vitest/coverage-v8": "^3.2.0",
    "eslint": "^10.4.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-svelte": "^3.19.0",
    "globals": "^17.6.0",
    "jsdom": "^26.0.0",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.3",
    "prettier-plugin-svelte": "^4.0.1",
    "svelte-check": "^4.4.7",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.60.0",
    "vitest": "^3.2.0"
  }
}
