{
  "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
  "build": {
    "features": ["mcp-bridge"]
  },
  "app": {
    "withGlobalTauri": true,
    "security": {
      "capabilities": [
        "default",
        {
          "identifier": "mcp-bridge-dev",
          "description": "dev-only MCP bridge access for shell inspection",
          "windows": ["main"],
          "permissions": ["mcp-bridge:default"]
        }
      ]
    }
  }
}
