[package]
name = "caelum-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"

[dependencies]
bevy_ecs = { version = "0.19.1", default-features = false, features = ["std"] }
serde = { version = "1.0.177", features = ["derive"] }
serde_json = "1.0"
